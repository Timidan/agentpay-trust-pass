export type CinematicSlug = "proof-corridor" | "signal-field" | "evidence-chamber";

export type CinematicRailItem = {
  id: string;
  eyebrow: string;
  title: string;
  body: string;
  href: string;
};
